[variables]
PYTHONPATH = "/app/src"
NIXPACKS_METADATA = "python,uv,nodejs"

[phases.setup]
nixPkgs = ["python312", "uv", "nodejs"]

[phases.install]
dependsOn = ["setup"]
cmds = ["uv sync --frozen"]

# Railway will use the custom Dockerfile instead 