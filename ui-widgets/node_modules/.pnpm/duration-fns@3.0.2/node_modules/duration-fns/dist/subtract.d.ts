import { Duration, DurationInput } from './types';
/**
 * Subtract durations from the first duration argument.
 *
 * @example
 * subtract({ days: 1 }, { hours: 12 })
 * // { days: 1, hours: -12 }
 */
export declare const subtract: (duration: DurationInput, ...durationsToSubtract: DurationInput[]) => Duration;
