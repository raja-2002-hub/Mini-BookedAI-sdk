import { Duration, DurationInput } from './types';
/**
 * Get the absolute value of a duration.
 *
 * @example
 * abs({ days: -1, seconds: 1 })
 * // { days: 1, seconds: -1 }
 */
export declare const abs: (duration: DurationInput) => Duration;
