import { Duration, DurationInput } from './types';
/**
 * Sum durations.
 *
 * @example
 * sum({ days: 1 }, { days: 2, hours: 12 })
 * { days: 3, hours: 12 }
 */
export declare const sum: (...durations: DurationInput[]) => Duration;
